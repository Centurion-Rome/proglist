# ProgList

Lists all installed Windows programs (both machine-wide and per-user) and exports them to a markdown table.

## Usage

Double-click `start.bat` or run:

```powershell
pwsh -ExecutionPolicy Bypass -File list-installed.ps1
```

Output is written to `installedprogs.md`.
